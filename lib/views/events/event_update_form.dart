import "package:flutter/material.dart";

class EventUpdateForm extends StatelessWidget {
  const EventUpdateForm({super.key});

  @override
  Widget build(final BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text("EventUpdateForm")),
      body: const Center(child: Column(children: [Text("EventUpdateForm")])),
    );
  }
}
