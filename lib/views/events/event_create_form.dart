import "package:flutter/material.dart";

class EventCreateForm extends StatelessWidget {
  const EventCreateForm({super.key});

  @override
  Widget build(final BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text("EventCreateForm")),
      body: const Center(child: Column(children: [Text("EventCreateForm")])),
    );
  }
}
