import "package:flutter/material.dart";

class FullScreenPFP extends StatelessWidget {
  const FullScreenPFP({super.key});

  @override
  Widget build(final BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text("FullScreenPFP")),
      body: const Center(child: Column(children: [Text("FullScreenPFP")])),
    );
  }
}
