import "package:flutter/material.dart";

class GroupchatCreateForm extends StatelessWidget {
  const GroupchatCreateForm({super.key});

  @override
  Widget build(final BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text("GroupchatCreateForm")),
      body:
          const Center(child: Column(children: [Text("GroupchatCreateForm")])),
    );
  }
}
