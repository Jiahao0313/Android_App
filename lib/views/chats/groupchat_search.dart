import "package:flutter/material.dart";

class GroupchatSearch extends StatelessWidget {
  const GroupchatSearch({super.key});

  @override
  Widget build(final BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text("GroupchatSearch")),
      body: const Center(child: Column(children: [Text("GroupchatSearch")])),
    );
  }
}
