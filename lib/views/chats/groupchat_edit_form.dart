import "package:flutter/material.dart";

class GroupchatUpdateForm extends StatelessWidget {
  const GroupchatUpdateForm({super.key});

  @override
  Widget build(final BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text("GroupchatUpdateForm")),
      body:
          const Center(child: Column(children: [Text("GroupchatUpdateForm")])),
    );
  }
}
