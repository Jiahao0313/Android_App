import "package:flutter/material.dart";

class RegisterAdditional extends StatelessWidget {
  const RegisterAdditional({super.key});

  @override
  Widget build(final BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text("RegisterAdditional")),
      body: const Center(child: Column(children: [Text("RegisterAdditional")])),
    );
  }
}
